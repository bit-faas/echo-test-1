provider:
  name: openfaas
  gateway: http://127.0.0.1:8080

functions:
  echo-test-1:
    lang: python3-flask-debian
    handler: ./function
    image: localhost:5000/echo-test-1:latest
    min_replicas: 1
    build_args:
      ADDITIONAL_PACKAGE: ""
    environment:
      DIAGNOSTICS: "false"

